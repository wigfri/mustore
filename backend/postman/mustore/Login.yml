info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{server}}/api/v1/auth/login/"
  body:
    type: json
    data: |-
      {
        "email": "test@vk.com",
        "password": "Urcopk@11_",
        "code": "656330"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
