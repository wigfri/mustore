info:
  name: Verify email
  type: http
  seq: 4

http:
  method: POST
  url: "{{server}}/api/v1/auth/verify-email"
  body:
    type: json
    data: |-
      {
        "email": "test@vk.com",
        "code": "057824"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
