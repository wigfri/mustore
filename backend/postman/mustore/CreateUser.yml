info:
  name: CreateUser
  type: http
  seq: 2

http:
  method: POST
  url: "{{server}}/api/v1/user/"
  body:
    type: json
    data: |-
      {
        "username": "test",
        "email": "test@vk.com",
        "password": "Urcopk@11_",
        "role": "base_user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
